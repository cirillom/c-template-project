{
    "tasks": [
        {
            "type": "shell",
            "label": "C: gcc build debug project",
            "command": "make debug",
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "type": "shell",
            "label": "C: gcc build project",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "type": "shell",
            "label": "C: gcc build and run project",
            "command": "make run",
            "options": {
                "cwd": "${workspaceFolder}/"
            }
        }
    ],
    "version": "2.0.0"
}